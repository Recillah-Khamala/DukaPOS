import React from 'react';
import { View, Text } from 'react-native';

export default function CreditDetailsTab({ sales }: { sales: any[] }) {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Credit Details Tab</Text>
    </View>
  );
}